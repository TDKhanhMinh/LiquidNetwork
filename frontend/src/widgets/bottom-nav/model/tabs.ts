/**
 * Re-export from shared config so existing imports keep working.
 * Source of truth: `@/shared/config/main-nav`.
 */
export {
  MAIN_TABS,
  isTabActive,
  type MainTab,
  type MainTabId,
} from "@/shared/config";
