/**
 * @deprecated Prefer `@/shared/api/errorHandler`.
 * Re-export for existing imports under `@/shared/lib`.
 */
export {
  normalizeApiError,
  notifyError,
  resolveErrorMessage,
  errorHandler,
} from "@/shared/api/errorHandler";
